{
  "name": "easing-functions",
  "version": "1.0.0",
  "description": "30+ funções de easing (ease-in, bounce, elastic)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["easing", "animation", "tween", "interpolation"],
  "author": "Tox",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
